# Playground

This is a tiny repository used for both automated end-to-end tests and
the autoevolve playground flow.

The validation command is:

```bash
python3 scripts/validate.py
```

The benchmark prints a `benchmark_score` value so synthetic experiments can
record a numeric metric in `EXPERIMENT.json`.
